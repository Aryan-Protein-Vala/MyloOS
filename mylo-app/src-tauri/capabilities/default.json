{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capabilities for MYLO's two windows. Every privileged operation (window styling, capture, input injection, keychain access) happens in Rust behind a hand-written command that validates its own input, so the webview needs no core window or store permissions at all.",
  "windows": ["main", "overlay"],
  "permissions": ["core:default", "log:default"]
}
